package ch9;
/*暗黙のコンストラクタ
 * 　とは？
 * 	Javaで全てのクラスはインスタンス化するときに
 * 	何かしらのコンストラクタを実行する必要がある…
 * 	というルールがある
 * 
 * なので…　全てのクラスは最低でも1つは
 * コンストラクタを持っている必要がある
 * 
 *コンストラクタの特例
 *1つもコンストラクタが定義されていない場合に限って
 *デフォルトコンストラクタが定義される。
 * 
 * 
 * */
public class Map {
public Map() { //newで実行したい処理はないが、一旦定義している（しなくてはいけない）
	
}
}
